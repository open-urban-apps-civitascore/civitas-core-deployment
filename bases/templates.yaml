---
templates:
  default:
    values:
      - values/{{ .Release.Name }}-base.yaml.gotmpl
      - values/{{ .Release.Name }}-{{ .Values.profile }}.yaml.gotmpl
      - ../../helpers/environment-merger.yaml.gotmpl
