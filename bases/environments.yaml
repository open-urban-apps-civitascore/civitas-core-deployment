---
environments:
  default:
    values:
      - locks/*.lock.yaml
      - environments/default/*.yaml
      - profile: local
  staging:
    values:
      - environments/default/*.yaml
      - profile: small
  production:
    values:
      - environments/default/*.yaml
      - environments/production/*.yaml
      - profile: large
