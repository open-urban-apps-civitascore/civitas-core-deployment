{{- if .Values.registry.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "apicurio.fullname" . }}-app
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "apicurio.labels" . | nindent 4 }}
    app.kubernetes.io/component: backend
  {{- with .Values.registry.app.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.registry.app.service.type | default "ClusterIP" }}
  ports:
  - port: {{ .Values.registry.app.service.port | default 8080 }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "apicurio.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: backend
{{- end }}
