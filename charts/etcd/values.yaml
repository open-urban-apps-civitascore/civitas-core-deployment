---
# Default values for etcd
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

# Override the name of the chart
nameOverride: ''
# Override the full name of the chart
fullnameOverride: ''

# Deployment profile: 'dev' or 'prod'
# Affects default resource allocations and replica counts
profile: prod

# Number of etcd replicas (use odd numbers)
# Description: Number of etcd replicas
# For production, use odd numbers (3, 5, 7) for quorum
# For development, 1 is acceptable
# Default: 3
replicas: 3

# Image configuration
image:
  # etcd image repository
  # Default: quay.io/coreos/etcd
  repository: quay.io/coreos/etcd
  pullPolicy: IfNotPresent
  # etcd image tag
  # Overrides the image tag whose default is the chart appVersion
  # Default: v3.5.17
  tag: 'v3.5.17'

imagePullSecrets: []

# etcd cluster configuration
etcd:
  # Cluster token for isolation
  # Description: Initial cluster token for the etcd cluster
  # Change this for each deployment to ensure cluster isolation
  # Default: etcd-cluster
  initialClusterToken: 'etcd-cluster'

  # Initial cluster state (new/existing)
  # Description: Initial cluster state
  # Use 'new' for new clusters, 'existing' for joining existing clusters
  # Default: new
  initialClusterState: 'new'

  # Auto compaction retention (hours)
  # Description: Auto compaction retention in hours
  # Automatically compact etcd keyspace history older than this value
  # Default: 1
  autoCompactionRetention: '1'

  # Snapshot count
  # Description: Number of committed transactions to trigger a snapshot
  # Default: 10000
  snapshotCount: 10000

  # Backend quota (bytes)
  # Description: Backend quota in bytes
  # Maximum backend database size (8GB default = 8589934592 bytes)
  # Default: 8589934592 (8GB)
  quotaBackendBytes: '8589934592'

  # Heartbeat interval (ms)
  # Description: Heartbeat interval in milliseconds
  # Time between heartbeat messages sent to followers
  # Default: 100
  heartbeatInterval: 100

  # Election timeout (ms)
  # Description: Election timeout in milliseconds
  # Time before starting a new leader election
  # Default: 1000
  electionTimeout: 1000

  # Maximum request bytes
  maxRequestBytes: '1572864'

  # Extra environment variables
  extraEnv: []
  #   - name: ETCD_DEBUG
  #     value: 'true'

  # Extra arguments for etcd
  extraArgs: []
  #   - --log-level=debug

# Authentication configuration
auth:
  # Enable RBAC authentication
  # Description: Enable RBAC authentication for etcd
  # Default: false
  enabled: true

  rbac:
    # Root password for the root user
    # Create a secret with key 'root-password' and reference it here
    rootPasswordSecret:
      name: 'etcd-root-password'
      key: 'etcd-root-password'

    # Custom roles to create with specific permissions
    # Roles are created before users and can have multiple permissions
    roles: []
    #   - name: apisix-readwrite
    #     permissions:
    #       - permission: readwrite  # Options: read, write, readwrite
    #         key: /apisix/          # Key or prefix path
    #         prefix: true           # Optional: true for prefix matching, false for single key (default: false)
    #   - name: app-readonly
    #     permissions:
    #       - permission: read
    #         key: /
    #         prefix: true
    #   - name: multi-permission-role
    #     permissions:
    #       - permission: readwrite
    #         key: /app1/
    #         prefix: true
    #       - permission: read
    #         key: /shared/config
    #         prefix: false         # Single key access only

    # Additional users to create with their roles
    # Each user should have a corresponding secret created
    # Users reference custom role names defined above
    users: []
    #   - username: apisix
    #     role: apisix-readwrite  # Reference to custom role name
    #     passwordSecret:
    #       name: etcd-apisix-password
    #       key: apisix-password
    #   - username: app1
    #     role: app-readonly
    #     passwordSecret:
    #       name: etcd-app1-password
    #       key: password

  # Authentication initialization job configuration
  # This job runs after StatefulSet deployment to set up RBAC users
  initJob:
    image:
      repository: registry.gitlab.com/civitas-connect/civitas-core/civitas-core-v2/civitas-core-deployment/etcdctl
      tag: '745818f'
      pullPolicy: IfNotPresent

    resources:
      requests:
        cpu: 100m
        memory: 64Mi
      limits:
        cpu: 200m
        memory: 128Mi

    # Job retry configuration
    backoffLimit: 5

    # Maximum time for job to complete (in seconds)
    activeDeadlineSeconds: 300

# TLS configuration
tls:
  # Enable TLS for client communication
  # Description: Enable TLS for client-to-server communication
  # Default: false
  enabled: false

  # Enable TLS for peer communication
  # Description: Enable TLS for peer-to-peer communication
  peer:
    enabled: false

  # Existing secret containing TLS certificates
  # Expected keys:
  #   - ca.crt: CA certificate
  #   - tls.crt: Server certificate
  #   - tls.key: Server private key
  #   - peer-ca.crt: Peer CA certificate (if peer TLS enabled)
  #   - peer-tls.crt: Peer certificate (if peer TLS enabled)
  #   - peer-tls.key: Peer private key (if peer TLS enabled)
  existingSecret: ''

# Persistent storage configuration
persistence:
  # Enable persistent storage
  # Description: Enable persistent storage for etcd data
  # Default: true
  enabled: true

  # Storage class name
  # Description: Storage class name for persistent volumes
  # If not set (empty string), the default storage class will be used
  # Default: "" (default)
  storageClass: ''

  # Access mode for the persistent volume
  accessMode: ReadWriteOnce

  # Size of persistent volume
  # Description: Size of the persistent volume
  # Production: 10Gi, Development: 1Gi
  # Default: 10Gi
  size: 10Gi

  # Annotations for the persistent volume claim
  annotations: {}

# Service configuration
service:
  # Service type for client access
  # Description: Service type for client access (ClusterIP, NodePort, LoadBalancer)
  # Default: ClusterIP
  type: ClusterIP

  # Client port
  clientPort: 2379

  # Peer port
  peerPort: 2380

  # Annotations for the service
  annotations: {}

  # Session affinity
  sessionAffinity: ClientIP

# Resource management
resources:
  requests:
    # CPU resource requests
    # Description: CPU resource requests
    # Default: 500m
    cpu: 500m
    # Memory resource requests
    # Description: Memory resource requests
    # Default: 512Mi
    memory: 512Mi
  limits:
    # CPU resource limits
    # Description: CPU resource limits
    # Default: 1000m
    cpu: 1000m
    # Memory resource limits
    # Description: Memory resource limits
    # Default: 1Gi
    memory: 1Gi

# Security context for the pod
podSecurityContext:
  fsGroup: 1000
  runAsNonRoot: true
  runAsUser: 1000

# Security context for containers
securityContext:
  allowPrivilegeEscalation: false
  capabilities:
    drop:
      - ALL
  readOnlyRootFilesystem: true
  runAsNonRoot: true
  runAsUser: 1000

# Pod disruption budget
podDisruptionBudget:
  # Enable PodDisruptionBudget
  # Description: Enable PodDisruptionBudget to ensure high availability during node maintenance
  # Default: true
  enabled: true
  # Minimum available pods during disruptions
  minAvailable: 1

# Affinity rules
affinity:
  # Pod anti-affinity to spread replicas across nodes
  podAntiAffinity:
    preferredDuringSchedulingIgnoredDuringExecution:
      - weight: 100
        podAffinityTerm:
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: etcd
          topologyKey: kubernetes.io/hostname

# Node selector
nodeSelector: {}

# Tolerations
tolerations: []

# Liveness probe configuration
livenessProbe:
  enabled: true
  initialDelaySeconds: 10
  periodSeconds: 10
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 3

# Readiness probe configuration
readinessProbe:
  enabled: true
  initialDelaySeconds: 5
  periodSeconds: 5
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 3

# Startup probe configuration
startupProbe:
  enabled: true
  initialDelaySeconds: 0
  periodSeconds: 10
  timeoutSeconds: 5
  successThreshold: 1
  failureThreshold: 30

# Graceful shutdown
# Increased to allow proper cluster member removal and data flush
terminationGracePeriodSeconds: 60

# Pod annotations
podAnnotations: {}

# Pod labels
podLabels: {}

# Service account
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use
  # If not set and create is true, a name is generated using the fullname template
  name: ''

# Priority class name
priorityClassName: ''

# Update strategy
updateStrategy:
  type: RollingUpdate
  rollingUpdate:
    partition: 0

# Extra volumes
extraVolumes: []
#   - name: extra-volume
#     emptyDir: {}

# Extra volume mounts
extraVolumeMounts: []
#   - name: extra-volume
#     mountPath: /extra

# Metrics configuration (for Prometheus integration)
metrics:
  enabled: false
  # Service monitor for Prometheus Operator
  serviceMonitor:
    enabled: false
    interval: 30s
    scrapeTimeout: 10s
    labels: {}
