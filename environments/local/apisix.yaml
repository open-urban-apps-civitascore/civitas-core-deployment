---
apisix:
  namespace: civitas
  enabled: true
  chartVersion: '2.12.0'
  image:
    repository: apache/apisix
    tag: '3.14.1-ubuntu'
  apisix:
    enable: true
    admin:
      domain: apisix.192.168.49.2.nip.io
      path: /
      credentials:
        # TODO: fix secret management
        secretName: apisix-admin-credentials
        secretAdminKey: admin
        secretViewerKey: viewer
        forceRecreateSecret: false
  replicaCount: 2
  resources:
    requests:
      memory: '256Mi'
      cpu: '250m'
    limits:
      memory: '1Gi'
      cpu: '1000m'
  timezone: 'Europe/Berlin'
  ingressController:
    enabled: true
