---
component: 'kafka'
parts:
  - name: operator
    extraLabels:
      operator: 'true'
      myLabel: 'true'
  - name: cluster
    needs:
      - kafka.operator
  - name: ui
    needs:
      - kafka.cluster
