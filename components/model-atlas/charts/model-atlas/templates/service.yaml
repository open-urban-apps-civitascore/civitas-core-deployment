apiVersion: v1
kind: Service
metadata:
  name: {{ include "model-atlas.fullname" . }}
  labels:
    {{- include "model-atlas.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "model-atlas.selectorLabels" . | nindent 4 }}
