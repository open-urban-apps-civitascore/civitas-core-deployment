---
model-atlas:
  podSelector:
    app.kubernetes.io/instance: model-atlas-model-atlas
    app.kubernetes.io/name: model-atlas
  policyTypes:
    - Ingress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app.kubernetes.io/name: portal
              app.kubernetes.io/component: backend
