---
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "authz.fullname" . }}-authz-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "authz.authz.labels" . | nindent 4 }}
data:
  application.yaml: |
    spring:
      application:
        name: authz-repository
      datasource:
        url: ${DATABASE_URL}
        # Production: use read-only account with SELECT on these tables only:
        # users, groups, group_members, assignments, roles, role_permissions, permissions
        username: ${DATABASE_USERNAME}
        password: ${DATABASE_PASSWORD}
        hikari:
          maximum-pool-size: 5
          read-only: true
      jpa:
        hibernate:
          ddl-auto: none
        open-in-view: false
        properties:
          hibernate:
            dialect: org.hibernate.dialect.PostgreSQLDialect
            default_schema: public
    server:
      port: {{ .Values.authz.service.port | default 8091 }}
    management:
      endpoints:
        web:
          exposure:
            include: health,info
      endpoint:
        health:
          show-details: never
    logging:
      level:
        de.civitascore.authz: INFO
        org.springframework.web: INFO
        org.hibernate.SQL: ${SQL_LOGGING:INFO}
