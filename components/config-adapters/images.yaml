---
configAdapters:
  adapters:
    # Open Urban Apps fork: same model-forge branch as the portal backend — the
    # branch turns pipelines and mappings into CORE artifacts, so the official
    # adapter cannot compile what this backend stores. Official upstream image:
    # registry.gitlab.com/civitas-connect/civitas-core/civitas-core-v2/civitas-core-platform/config-adapter:1.0.2
    repository: 'ghcr.io/open-urban-apps-civitascore/config-adapter'
    tag: 'mf-2'
