apiVersion: v1
kind: Service
metadata:
  name: {{ include "config-adapter.fullname" . }}
  labels:
    {{- include "config-adapter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "config-adapter.selectorLabels" . | nindent 4 }}
