{{ $root := . }}
{{ range $component, $config := .Values.databases }}
---
apiVersion: postgresql.cnpg.io/v1
kind: Database
metadata:
  name: {{ $config.name }}
  labels:
    {{- include "database.labels" $root | nindent 6 }}
    ## TODO: Add component label?
spec:
  cluster:
    name: {{ $root.Values.cluster.name }}
  name: {{ $config.name }}
  owner: {{ $config.user }}
{{- end }}
