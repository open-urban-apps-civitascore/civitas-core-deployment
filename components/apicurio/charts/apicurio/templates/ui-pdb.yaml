{{- if and .Values.registry.enabled .Values.registry.ui.enabled .Values.registry.ui.podDisruptionBudget.enabled -}}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "apicurio.fullname" . }}-ui
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "apicurio.labels" . | nindent 4 }}
    app.kubernetes.io/component: ui
spec:
  {{- if .Values.registry.ui.podDisruptionBudget.minAvailable }}
  minAvailable: {{ .Values.registry.ui.podDisruptionBudget.minAvailable }}
  {{- else if .Values.registry.ui.podDisruptionBudget.maxUnavailable }}
  maxUnavailable: {{ .Values.registry.ui.podDisruptionBudget.maxUnavailable }}
  {{- else }}
  minAvailable: 1
  {{- end }}
  selector:
    matchLabels:
      {{- include "apicurio.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: ui
{{- end }}
