annotations: []

labels: []

name: {{ default "keycloak" .Values.databases.keycloak.name }}

database:
  allowConnections: true
  builtinLocale:
  cluster:
    name: "{{ .Values.embeddedDatabase.cluster.name }}"
  collationVersion:
  connectionLimit:
  databaseReclaimPolicy:
  encoding:
  ensure: present
  extensions: []
  icuLocale:
  icuRules:
  isTemplate:
  locale:
  localeCType:
  localeCollate:
  localeProvider:
  name: {{ default "keycloak" .Values.databases.keycloak.name }}
  owner: {{ default "keycloak" .Values.databases.keycloak.user }}
  schemas: []
  tablespace:
  template:
  createSecret:
    enabled: false
    name: {{ .Values.databases.keycloak.secret.name }}
    username: {{ default "keycloak" .Values.databases.keycloak.user }}